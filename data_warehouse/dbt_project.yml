name: 'data_warehouse'

config-version: 2
version: '0.1'

profile: 'default'

model-paths: ["models"]

target-path: "target"  # directory which will store compiled SQL files


models:
  data_warehouse:
    marts:
      feature_generation:
        +dataset: feature_generation
      training_table_generation:
        +dataset: training_table_generation
    staging:
      restaurant_data:
        +dataset: staging

    